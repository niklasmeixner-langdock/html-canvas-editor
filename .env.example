# Optional. If set, /mcp requires Authorization: Bearer <token>
MCP_AUTH_TOKEN=

# Railway sets this. Local default is 8788.
PORT=8788

# Optional. Public base URL used for download links opened from the MCP host.
# Defaults to RAILWAY_PUBLIC_DOMAIN or the request host.
PUBLIC_URL=
